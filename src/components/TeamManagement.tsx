export default function TeamManagement({ coachId }: { coachId: string }) {
  return (
    <div className="p-6 bg-gray-800 rounded-xl text-center text-white text-lg font-bold">
      Team Management coming soon for coach: {coachId}
    </div>
  );
}