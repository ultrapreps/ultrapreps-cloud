export default function AdminAnalytics({ adminId }: { adminId: string }) {
  return (
    <div className="p-6 bg-gray-800 rounded-xl text-center text-white text-lg font-bold">
      Admin Analytics coming soon for admin: {adminId}
    </div>
  );
}